export type PointOfSale = {
  id: number;
  name: string;
  slug: string;
};

export type Provider = {
  id: number;
  name: string;
  slug: string;
};

export type ProductType = {
  id: number;
  name: string;
  slug: string;
};
